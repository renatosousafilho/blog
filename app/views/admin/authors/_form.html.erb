<%= form_for(@author, :url => {:controller=>"admin/authors", :action => :create}) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<br/> 
	<%= f.label :bio %>
	<%= f.text_area :bio %>

	<br/>
	<%= f.submit "Salvar" %>
<% end %>