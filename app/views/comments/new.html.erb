<%= form_for @comment do |f| %>
	<div class="field">	
		<%= f.label :body, "Digite aqui seu comentario" %><br/>
		<%= f.text_area :body %>
	</div>

	<div class="field">
		<%= f.label :post_id, "Selecione uma noticia" %>
		<br />

		<%= select_tag "comment[post_id]", options_for_select(Post.options) %>
	</div>
	<br/>

	<%= f.submit "Salvar comentário" %>
<% end %>